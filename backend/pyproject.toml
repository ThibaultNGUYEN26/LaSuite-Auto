[project]
name = "auto-backend"
version = "0.1.0"
description = "Backend for the Auto conversational chatbot"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.9",
]

[tool.uv]
package = false
