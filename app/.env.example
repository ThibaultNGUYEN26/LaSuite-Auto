# Full URL (protocol + host + port) where the Auto backend is reachable.
# Must match the backend's own BACKEND_URL (backend/.env) — change the host
# if the backend runs on a different machine than this app.
BACKEND_URL=http://127.0.0.1:8000
