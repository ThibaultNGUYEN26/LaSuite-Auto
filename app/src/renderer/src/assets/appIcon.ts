export { default } from '../../../../resources/icon.svg'
// Raw markup, for recoloring the icon (e.g. via `fill: currentColor`) —
// the svg's own path fills can't be overridden once it's loaded as a plain <img> src.
export { default as appIconMarkup } from '../../../../resources/icon.svg?raw'
